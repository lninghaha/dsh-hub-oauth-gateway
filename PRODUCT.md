# Product

## Register

product

## Users

使用 DeepSeek Harness 管理多个编码订阅、用量、成本与本地 Gateway 的开发者和操作者。他们需要在一个可信入口中快速判断账户是否可用、数据是否新鲜、成本是否准确，以及下一步该处理什么。

## Product Purpose

作为 DSH 内的统一用量中心，汇总订阅 OAuth、配额、成本、定价与 Gateway 能力。成功意味着首次使用者能顺利完成连接和配置，日常使用者能迅速识别异常与过期数据，并且 DSH 更新或可选服务缺失不会拖垮整个插件。

## Brand Personality

稳健、清晰、务实。视觉与交互遵循 DSH 产品语言，信息密度服务于判断，不以装饰制造“高级感”。

## Anti-references

避免模板化 SaaS 指标卡墙、彩色渐变数据装饰、宿主 DOM/CSS 侵入、静默失败、把零值与未加载混为一谈，以及在未确认时覆盖用户编辑内容。

## Design Principles

1. 数据新鲜度和来源与数据值同等重要。
2. 首次使用围绕“连接订阅、同步定价、配置 Gateway”逐步引导。
3. 修改先保留草稿并展示影响，保存结果在操作附近反馈。
4. DSH 能力按需增强，缺失时保留核心用量与诊断入口。
5. 共装时由 Hub 统一承载完整体验，独立插件仍保持可恢复能力。

## Accessibility & Inclusion

设置、图表、标签页和确认流程支持键盘、屏幕阅读器、缩放、窄屏及 reduced-motion；状态、趋势与错误不能只靠颜色表达。
