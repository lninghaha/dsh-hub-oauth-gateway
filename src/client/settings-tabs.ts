export type SettingsTabId = "display" | "providers" | "fees" | "credentials";

export const SETTINGS_TABS: readonly SettingsTabId[] = Object.freeze([
	"display",
	"providers",
	"fees",
	"credentials",
]);
